{
   "version": "0.2.0",
   "configurations": [
       {
           "name": "Docker .NET Core Attach (Preview)",
           "type": "docker",
           "request": "attach",
           "platform": "netCore",
           "sourceFileMap": {
               "/src": "${workspaceFolder}"
           },
           "netCore": {
            "appProject": "${workspaceFolder}/1 - Application/MyFirstProject.Api/MyFirstProject.Api.csproj"
        }
       }
    ]
}